'use server';

/**
 * Market data fetching has been moved to client-side src/lib/api-service.ts 
 * to support static export on GitHub Pages.
 */
export async function getMarketData() {
  return null;
}
